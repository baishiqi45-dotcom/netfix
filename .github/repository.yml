repository:
  name: netfix
  description: "Local-first macOS network triage for AI/dev tools: diagnose DNS, proxy, node, IPv6, TLS, and target service issues before changing config."
  private: false
  has_issues: true
  has_projects: false
  has_wiki: false
  default_branch: main
  topics:
    # platform / runtime
    - macos
    - apple-silicon
    - swiftui
    - python
    - homebrew
    # what it does
    - network-diagnostics
    - network-triage
    - network-monitor
    - dns
    - tls
    - ipv6
    # AI / dev tool reachability (these are the highest-traffic search terms in 2026)
    - codex
    - claude
    - cursor
    - kimi
    - openai
    - chatgpt
    - github
    - developer-tools
    # agent / protocol
    - mcp
    - model-context-protocol
    - mcp-server
    # privacy / posture
    - offline-first
    - privacy
    - local-first
    # ecosystems users already know
    - clash
    - mihomo
    - sing-box